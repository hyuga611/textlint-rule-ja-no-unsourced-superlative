# Changelog

## 0.2.0

別のモデル（GPT-5.4）にルール本体だけを渡し、「これを黙らせる広告文を作れ」と指示して
返ってきた14件を実際に通した。**14件すべてで黙った。** そのうち5件を塞いだ。

このルールが壊れる壊れ方は「鳴りすぎ」だと考えて設計してきたが、鳴らない側にも
穴があった。景表法の文脈では、見逃しは不当表示がそのまま公開されるということになる。

### 西暦4桁なら何でも根拠として扱っていた

```
顧客満足度No.1                    → 報告する
顧客満足度No.1。2026年春の新プラン。  → 黙る
```

コメントには「『2026年3月時点』のような調査時点の表記」と書いてあったのに、実装は
`(19|20)\d{2}\s*年` で、年号の言及なら何でも通していた。**広告文は年号だらけ**なので、
実際のLPほど鳴らなくなる。書いてある意図に実装を寄せ、「時点」「現在」を伴う場合だけに
絞った。調査年の明示（`2026年3月 当社調べ`）は従来どおり `調べ` `調査` の側で拾う。

「年号があるなら時点を書く意識はある」という元の判断は、脚注を書く人ほど怒られる状況を
避けるためのものだった。そこは変わっていない——本物の脚注には `調べ` `調査` `時点` の
どれかが入るので、今も黙る。

### 全角表記が素通りしていた

`Ｎｏ．１` `ｎ＝1,200`。日本語の広告では全角が普通に使われる。半角に寄せた写しの上で
判定するようにした。1文字＝1文字の変換なので報告位置はずれない。報告文には書き手が
実際に書いた形をそのまま出す（`Ｎｏ．１` を `No.1` と言い換えない）。

### 語彙の追加

`シェア首位` `業界最低価格` `地域最低価格` `最低価格` `ナンバー・ワン`。

`首位` 単体は入れていない。順位表やスポーツで普通に使う語で、既定に入れると
「リーグ首位」「首位打者」で鳴る。`シェア首位` なら市場での一番の主張だと誰が読んでも取れる。

### 塞いでいないもの

- **`※` `調査` `対象者` が、無関係な文脈でも根拠とみなされる。**
  「※送料無料キャンペーン」「調査不要の簡単申込み」「対象者限定で初回50%OFF」で黙る。
  ここを締めると本物の脚注まで鳴らしかねず、誤検知と引き換えになる。判断が要るので残した。
- **強調が語の内部を分断すると見つからない。** `No.**1**` `日本**一**`。
  Str ノードが割れるため。`**顧客満足度No.1**` のようにフレーズ全体を囲む分には問題ない。
- `最高` `トップ` 系は従来どおり既定では鳴らさない（非広告用法が普通にあるため）。

### 誤検知

非広告の日本語・英語 31,352 文字に当てて、新しく増えた検出は 0 件。`corpus/` も 0 件のまま。
